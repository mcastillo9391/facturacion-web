name: Backend Azure

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Restore
        run: dotnet restore backend/Facturacion.API/Facturacion.API.csproj

      - name: Build
        run: dotnet build backend/Facturacion.API/Facturacion.API.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish backend/Facturacion.API/Facturacion.API.csproj -c Release -o publish --no-build

      - uses: actions/upload-artifact@v4
        with:
          name: app
          path: publish

  deploy:
    runs-on: ubuntu-latest
    needs: build

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_CF13311E557946019C752E3B0F82CA98 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_BCCD916844EF4DA88B3BC36F0552C8AE }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_3504BB481AB24BBFB700749FBC1B6844 }}

      - uses: azure/webapps-deploy@v3
        with:
          app-name: sumptuous-fact
          package: .
